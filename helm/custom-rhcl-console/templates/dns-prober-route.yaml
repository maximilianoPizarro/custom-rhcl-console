{{- if and .Values.dnsProber.enabled .Values.dnsProber.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "custom-rhcl-console.dnsProber" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "custom-rhcl-console.labels" . | nindent 4 }}
    {{- include "custom-rhcl-console.dnsProberSelectorLabels" . | nindent 4 }}
spec:
  {{- if .Values.dnsProber.route.host }}
  host: {{ .Values.dnsProber.route.host | quote }}
  {{- end }}
  to:
    kind: Service
    name: {{ include "custom-rhcl-console.dnsProber" . }}
  port:
    targetPort: http
  tls:
    termination: {{ .Values.dnsProber.route.tls.termination }}
    insecureEdgeTerminationPolicy: {{ .Values.dnsProber.route.tls.insecureEdgeTerminationPolicy }}
{{- end }}
